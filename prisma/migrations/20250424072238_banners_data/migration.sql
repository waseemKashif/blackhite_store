-- CreateTable
CREATE TABLE "Banners" (
    "id" TEXT NOT NULL,
    "webImage" TEXT NOT NULL,
    "mobImage" TEXT NOT NULL,
    "alt" TEXT NOT NULL,

    CONSTRAINT "Banners_pkey" PRIMARY KEY ("id")
);
